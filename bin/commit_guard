#!/usr/bin/env ruby

require 'optparse'
require 'commit_guard'
options = {}

OptionParser.new do |opts|
  opts.banner = "Usage: commit_guard [project_path] [options]"
end
project_path =  ARGV[0] || ENV['PWD']
runner = CommitGuard::Runner.new(project_path)
runner.run

exit runner.exit_status
